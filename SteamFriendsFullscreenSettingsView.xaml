<UserControl x:Class="SteamFriendsFullscreen.SteamFriendsFullscreenSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             d:DesignWidth="800"
             Background="Transparent"
             Foreground="#EDEDED">

    <UserControl.Resources>

        <!--Cards-->

        <Style x:Key="CardBorder" TargetType="Border">
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Background" Value="#03f5f5f5"/>
            <Setter Property="BorderBrush" Value="#32FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="TextElement.Foreground" Value="#EDEDED"/>
        </Style>

        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="Label" TargetType="TextBlock">
            <Setter Property="Opacity" Value="0.85"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <Style x:Key="Help" TargetType="TextBlock">
            <Setter Property="Opacity" Value="0.65"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="0,6,0,0"/>
        </Style>

        <Style x:Key="Divider" TargetType="Separator">
            <Setter Property="Opacity" Value="0.25"/>
            <Setter Property="Margin" Value="0,12,0,12"/>
        </Style>

        <Style x:Key="HintPill" TargetType="Border">
            <Setter Property="CornerRadius" Value="999"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Background" Value="#16000000"/>
            <Setter Property="BorderBrush" Value="#2AFFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="PillText" TargetType="TextBlock">
            <Setter Property="Opacity" Value="0.85"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style x:Key="NeedsApiKeyBlock" TargetType="FrameworkElement">
            <Setter Property="IsEnabled" Value="True"/>
            <Setter Property="Opacity" Value="1"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Settings.SteamApiKey}" Value="">
                    <Setter Property="IsEnabled" Value="False"/>
                    <Setter Property="Opacity" Value="0.5"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="#EDEDED"/>
            <Setter Property="Background" Value="#12000000"/>
            <Setter Property="BorderBrush" Value="#2AFFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="MinHeight" Value="34"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#EDEDED"/>
            <Setter Property="Opacity" Value="0.95"/>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="#EDEDED"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6">
                            <ContentPresenter />
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#2AFFFFFF"/>
                            </Trigger>

                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#3A66B7FF"/>
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="12" Orientation="Vertical">

            <!-- Header -->
            <TextBlock Text="{DynamicResource LOCSteamFriends_Title}"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Margin="0,0,0,6"/>

            <TextBlock Text="{DynamicResource LOCSteamFriends_Subtitle}"
                       Opacity="0.7"
                       TextWrapping="Wrap"
                       Margin="0,0,0,14"/>

            <!-- SETUP -->
            <Border Style="{StaticResource CardBorder}">
                <StackPanel>
                    <TextBlock Text="{DynamicResource LOCSteamFriends_Setup}" Style="{StaticResource SectionTitle}"/>

                    <TextBlock Text="{DynamicResource LOCSteamFriends_ApiKey}" Style="{StaticResource Label}"/>
                    <TextBox Text="{Binding Settings.SteamApiKey, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,0,10"/>

                    <TextBlock Text="{DynamicResource LOCSteamFriends_Profile}" Style="{StaticResource Label}"/>
                    <TextBox Text="{Binding Settings.SteamId64, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,0,0"/>

                    <TextBlock Style="{StaticResource Help}"
                               Text="{DynamicResource LOCSteamFriends_ProfileHelp}"/>
                </StackPanel>
            </Border>

            <!-- DISPLAY -->
            <Border Style="{StaticResource CardBorder}">
                <StackPanel>
                    <TextBlock Text="{DynamicResource LOCSteamFriends_Display}" Style="{StaticResource SectionTitle}"/>

                    <StackPanel Style="{StaticResource NeedsApiKeyBlock}">
                        <TextBlock Text="{DynamicResource LOCSteamFriends_OnlineInfo}" Style="{StaticResource Label}"/>

                        <CheckBox Content="{DynamicResource LOCSteamFriends_ShowOffline}"
                                  IsChecked="{Binding Settings.ShowOffline, UpdateSourceTrigger=PropertyChanged}"
                                  Margin="0,10,0,0"/>
                        <TextBlock Text="{DynamicResource LOCSteamFriends_OfflineInfo}" Style="{StaticResource Help}"/>

                    </StackPanel>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
